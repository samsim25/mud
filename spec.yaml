name: avalon-web-app
services:
  - name: avalon-web
    github:
      repo: samsim25/mud
      branch: master
    # Let the DigitalOcean buildpack handle build and run; specify solution via env
    envs:
      - key: DOTNET_BUILD_SOLUTION_PATH
        value: Avalon.Web.sln
    http_port: 8080
